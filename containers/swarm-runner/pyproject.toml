[project]
name = "twinstore-swarm-runner"
version = "0.0.1"
description = "Per-run swarm orchestrator: fans out twin reactions to K2 Think and consolidates actions"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27.0",
    "pydantic>=2.9.0",
    "openai>=1.50.0",
]
