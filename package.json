{
  "name": "twinstore",
  "version": "0.0.1",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev:dashboard": "pnpm --filter dashboard dev",
    "dev:backend": "cd apps/backend && uv run uvicorn main:app --reload --port 8000",
    "build": "pnpm -r build"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
